[package]
name = "gearx_input"
version = "0.1.0"
edition = "2021"
description = "GearX input module — keyboard, mouse, gamepad handling"

[dependencies]
gearx_kernel = { path = "../../kernel" }
linkme = ">=0.3.32"
anyhow = "1"
